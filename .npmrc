# React Native / Expo requieren un node_modules plano (estilo npm).
# pnpm por defecto usa symlinks aislados que rompen la resolución de
# dependencias transitivas como metro-runtime. node-linker=hoisted lo arregla.
node-linker=hoisted
